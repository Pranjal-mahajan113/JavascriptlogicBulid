function multiplication(multiplenum) {
  let result = 1;
  for (let i = 1; i <= 10; i++) {

    console.log(`${multiplenum} x ${i} = ${multiplenum * i}`);

    // console.log(i);
  }
}
console.log(multiplication(2));

//2X1=2
//2X2=4
